{
    // Server build is made in two times
    // 1. TypeScript type check and keep intact the source code
    // 2. Babel transform this code for the runtime:
    // * Presets for code transformation: React / es2015
    // * Plugins for server side rendering: syntax-dynamic-import / dynamic-import-node / react-loadable/babel
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "outDir": "../temp/server"
    },
    "include": [
        "../source/server/index.tsx"
    ]
}